namespace Spendly.Domain.Enums
{
    /// <summary>
    /// Frecuencia de repetición de un gasto recurrente.
    /// </summary>
    public enum RecurrenceFrequency
    {
        Daily = 1,      // Cada día
        Weekly = 2,     // Cada semana
        Monthly = 3,    // Cada mes
        Yearly = 4      // Cada año
    }
}
